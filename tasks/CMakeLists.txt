# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(vispTaskLib)
ADD_LIBRARY(${VISP_TASKLIB_NAME} SHARED 
    ${VISP_TASKLIB_SOURCES})
add_dependencies(${VISP_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${VISP_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${VISP_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${VISP_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VISP_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${VISP_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${VISP_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VISP_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VISP_TASKLIB_HEADERS}
    DESTINATION include/orocos/visp)

